# The frozen contract is setup-like and immutable: TRACK its cheap metadata
# (selection.json, contract.json, reference.py) so prep is reproducible and
# auditable. IGNORE only the large, regenerable artifacts below.
targets/**/inputs.pt
targets/**/golden.pt
targets/**/profile.json
targets/**/inductor/
targets/**/kernel/build/
targets/**/loop_history.json
targets/**/result.json
targets/**/weights.pt
targets/**/assets/
__pycache__/
*.pyc
# Secrets — NEVER tracked. HF_TOKEN etc. go here (synced to host, env-file'd
# into the container by config.yaml: exec.docker_run_extra).
secrets.env
# Live session scratch state, not project history (canonical doc = AGENTS.md).
HANDOFF.md
