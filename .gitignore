# configs/ is per-machine, regenerable (config.json from parquet+env;
# everything else derived). Never committed; the solve loop snapshots
# and restores it. Fully ignored so git status/clean stay correct.
configs/
data/
docs/failure_modes.md
__pycache__/
*.pyc
secrets.env
