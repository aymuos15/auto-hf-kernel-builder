# tasks/ is fully derived: core/prepare.py regenerates the frozen contract,
# the loop regenerates kernel.py/result.json. The repo tracks the engine
# only — per-task artifacts are local/regenerable, never history.
tasks/
data/
__pycache__/
*.pyc
secrets.env
HANDOFF.md
